name: caravaggio_ui
description: "A comprehensive UI package designed to streamline your Flutter app development process."
version: 0.0.9
homepage: "https://github.com/lorenzo9598/caravaggio_ui"

environment:
  sdk: ">=3.2.2 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  percent_indicator: ^4.2.3
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
